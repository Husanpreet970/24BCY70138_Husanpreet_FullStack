//Experiment 3 — React Router and Axios API Integration
//Problem Statement
//Develop a React application that demonstrates client-side routing, dynamic URL parameters, query parameters, Axios API communication, Axios interceptors, custom React Hooks, useMemo(), and passing data between parent and child components using props.

import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";
const root = ReactDOM.createRoot(
  document.getElementById("root")
);
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
